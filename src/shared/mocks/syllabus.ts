import type { SyllabusModule } from '@/entities/syllabus';

// Mock data reemplazado por carga real desde CRONOGRAMA*.xlsx
// Ver /ingestion → zona "Cronograma del Curso"
export const mockSyllabus: SyllabusModule[] = [];
